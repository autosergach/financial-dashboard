services:
  - type: web
    name: financial-dashboard-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CACHE_TTL_MS
        value: 300000
